
'use strict';

var test = require('NativeModules').LoginModule;

// LoginModule.show(
// 	'some message'
// );

var LoginModule = {


  show: function (
    message: string
  ): void {
    test.show(message);
  },

  getCredentials: function (
  	username: string,
  	password: string
  ): void {
  	test.getCredentials(username, password);
  },

};

module.exports = LoginModule;